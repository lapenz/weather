<!--This is the form that the user sends the address in order to get the weather info.-->
<!--One thing that could be done here is to get the location through geolocation on the browser.-->
<%= form_with url: "/", class: 'row', method: :get do |form| %>
  <div class="col-11">
    <%= form.text_field :query, value: params[:query], class: 'form-control', placeholder: "Enter your Zipcode", required: true %>
  </div>
  <div class="col-1">
    <button type="submit" class="btn btn-dark">
      <!-- This SVG is just for demo, the Rails assets should be used instead-->
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-search" viewBox="0 0 16 16">
        <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001c.03.04.062.078.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1.007 1.007 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0z"/>
      </svg>
    </button>
  </div>
<% end %>